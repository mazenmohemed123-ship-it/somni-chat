{
  "name": "somni-chat",
  "version": "1.0.0",
  "private": true,
  "description": "Universal, framework-agnostic messaging + calling infrastructure",
  "workspaces": [
    "packages/*",
    "packages/chat-adapters/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "npm --workspace @somni/chat-core test && npm --workspace @somni/chat-call test && npm --workspace @somni/notifications test && npm --workspace @somni/analytics test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.13.0",
    "typescript": "^5.4.0",
    "prettier": "^3.2.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
